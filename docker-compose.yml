services:
  oss_backend:
    build:
      context: .
      dockerfile: oss_assistant/Dockerfile
    ports:
      - "8000:8000"
    env_file: .env

  frontier_backend:
    build:
      context: .
      dockerfile: frontier_assistant/Dockerfile
    ports:
      - "8001:8001"
    env_file: .env
